import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const insights = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-white">insights</Text>
      </View>
    </SafeAreaView>
  );
};

export default insights;
